(function(r,f){const a=f();if(typeof module==='object'&&module.exports)module.exports=a;r.KoRishonGhostReplay=a})(globalThis,function(){const clone=x=>JSON.parse(JSON.stringify(x));function compare(current,ghost){if(!Array.isArray(current)||!Array.isArray(ghost))throw Error('REPLAY_INVALID');const keys=new Set([...current,...ghost].map(x=>x.key));return[...keys].map(key=>{const a=current.find(x=>x.key===key),b=ghost.find(x=>x.key===key);return{key,currentMs:a?.timeMs??null,ghostMs:b?.timeMs??null,deltaMs:a&&b?a.timeMs-b.timeMs:null,status:!a?'missed':!b?'extra':a.timeMs===b.timeMs?'matched':'diverged'}})}return{compare};});
