(function(r,f){const a=f();if(typeof module==='object'&&module.exports)module.exports=a;r.KoRishonUnexpectedEvents=a})(globalThis,function(){function rand(seed){let x=seed|0;return()=>{x=Math.imul(x^x>>>16,0x45d9f3b);return(x>>>0)/4294967296}}class Engine{constructor({seed,definitions}){this.r=rand(seed);this.q=definitions.map((d,i)=>({...d,atMs:Math.round(d.minMs+this.r()*(d.maxMs-d.minMs)),i})).sort((a,b)=>a.atMs-b.atMs||a.i-b.i);this.fired=[]}advance(ms){const out=this.q.filter(x=>x.atMs<=ms&&!x.fired);for(const x of out){x.fired=true;this.fired.push({id:`unexpected:${this.fired.length}`,type:x.type,atMs:x.atMs,payload:x.payload||{}})}return this.fired.slice(-out.length)}snapshot(){return{pending:this.q.filter(x=>!x.fired).length,fired:this.fired.slice()}}}return{Engine}});
