'use strict'; const {envelope,req,arr,sha}=require('./shared'); const {ValidationError}=require('./validation-result');
const KINDS=['MESH_3D','RIGGED_MESH_3D','PROCEDURAL_MESH','IMAGE_2D','PANORAMA_2D','BILLBOARD_LOD','EFFECT_ONLY','UI_ONLY'];
function validateAsset(r){envelope(r,'ASSET');req(r,['artifact','source','representationKind','roles','coordinateContract','structureRefs','validTransformDomain','evidenceRefs']);req(r.artifact,['uri','digest','byteLength','mediaType','format']);if(r.artifact.digest.algorithm!=='sha256')throw new ValidationError('P1-G02','digest algorithm');sha(r.artifact.digest.value);if(!KINDS.includes(r.representationKind))throw new ValidationError('P1-G01','representationKind');arr(r.roles,'roles',true);arr(r.evidenceRefs,'evidenceRefs');if((/png|jpeg|image/i.test(r.artifact.mediaType))&&r.representationKind==='MESH_3D')throw new ValidationError('P1-G03','image cannot be mesh');return r;}
module.exports={validateAsset,ASSET_KINDS:Object.freeze(KINDS)};
