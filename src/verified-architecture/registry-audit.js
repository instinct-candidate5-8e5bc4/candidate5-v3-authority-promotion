'use strict'; const {canonicalJson,digest}=require('./canonical-json'); const {ValidationError}=require('./validation-result'); function exportAudit(registries){const records=registries.flatMap(r=>r.snapshot());for(const r of records)for(const k of ['lifecycleState','failureReasons'])if(r[k]===undefined)throw new ValidationError('P1-G15',`audit missing ${k}`);const snapshot={placementReady:false,records,unresolvedProofGaps:records.filter(r=>r.failureReasons.length).map(r=>({recordId:r.recordId||r.packageId,failureReasons:r.failureReasons}))};return Object.freeze({canonical:canonicalJson(snapshot),digest:digest(snapshot),snapshot});}module.exports={exportAudit};
