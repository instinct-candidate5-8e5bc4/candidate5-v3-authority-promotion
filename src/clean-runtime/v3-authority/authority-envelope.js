'use strict';
const {digest}=require('../contracts/canonical');const {deepFreeze}=require('../contracts/world-state');
const HEX=/^[0-9a-f]{64}$/;function clone(x){return structuredClone(x)}function seal(body,field='envelopeDigest'){const b=clone(body);b[field]=digest(b);return deepFreeze(b)}function validRef(r){return !!r&&typeof r==='object'&&typeof Object.values(r)[0]==='string'&&Number.isInteger(r.revision)&&r.revision>0&&HEX.test(r.digest||'')}
function refEqual(a,b){return !!a&&!!b&&a.revision===b.revision&&a.digest===b.digest&&Object.keys(a).filter(k=>k.endsWith('Id')).every(k=>a[k]===b[k])}
function contextForObligation(envelope,o){if(!envelope||!o)return {status:'INCOMPLETE_AUTHORITY'};const a=o.authorityRequirements||{},pick=(src,ids)=>Object.fromEntries((ids||[]).map(id=>[id,src?.[id]]).filter(([,v])=>v));const ctx={frames:pick(envelope.frames,a.geometryFrameIds),definitions:pick(envelope.definitions,a.geometryIds),bodies:pick(envelope.bodies,a.bodyIds),contactRegions:pick(envelope.contactRegions,a.contactRegionIds),boundaryFeatures:pick(envelope.boundaryFeatures,a.boundaryFeatureIds),authoritativeGeometryRefs:pick(envelope.authority.geometryRefs,a.geometryIds),authoritativeGeometryFrameRefs:pick(envelope.authority.geometryFrameRefs,a.geometryIds),authoritativeBoundaryFeatureRefs:pick(envelope.authority.boundaryFeatureRefs,a.boundaryFeatureIds)};for(const [k,ids] of [['definitions',a.geometryIds],['bodies',a.bodyIds],['contactRegions',a.contactRegionIds],['boundaryFeatures',a.boundaryFeatureIds]])if((ids||[]).some(id=>!ctx[k][id]))return {status:'INCOMPLETE_AUTHORITY',missingKind:k};const body={schemaVersion:'v3-obligation-context/1.0.0',obligationId:o.obligationId,context:ctx};return deepFreeze({status:'READY',...body,contextDigest:digest(body)})}
module.exports={HEX,seal,validRef,refEqual,contextForObligation};
