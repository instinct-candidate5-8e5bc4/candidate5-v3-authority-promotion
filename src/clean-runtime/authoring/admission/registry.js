'use strict';const {matches}=require('./definition-ref'),{validateEnvelope}=require('./envelope');
function admit({definitionType,definition,envelope,expectedScope}){const v=validateEnvelope(envelope);if(v.status!=='VALIDATED')return v;if(v.envelope.lifecycleState!=='VERIFIED_FOR_SLICE')return {status:'REJECTED',failure:{code:'NOT_VERIFIED_FOR_SLICE'}};if(expectedScope&&v.envelope.reviewScope!==expectedScope)return {status:'REJECTED',failure:{code:'REVIEW_SCOPE_MISMATCH'}};if(!matches(v.envelope.definitionRef,definitionType,definition))return {status:'REJECTED',failure:{code:'DEFINITION_REF_MISMATCH'}};return Object.freeze({status:'ADMITTED',definition,envelope:v.envelope})}
function supersede({activeEnvelope,supersedingEnvelopeId,evidenceRefs=[]}){return require('./envelope').transitionEnvelope(activeEnvelope,'SUPERSEDED',{supersession:{supersedingEnvelopeId,evidenceRefs}})}module.exports={admit,supersede};
