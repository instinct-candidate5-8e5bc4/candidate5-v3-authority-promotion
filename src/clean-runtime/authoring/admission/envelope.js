'use strict';const {digest,canonicalBytes}=require('../contracts/canonical'),{definitionRef}=require('./definition-ref');
const STATES=Object.freeze(['AUTHORED_NEW_DRAFT','VALIDATED','REVIEWED','VERIFIED_FOR_SLICE','REJECTED','SUPERSEDED','UNKNOWN']);const NEXT=Object.freeze({AUTHORED_NEW_DRAFT:['VALIDATED','REJECTED','UNKNOWN'],VALIDATED:['REVIEWED','REJECTED','SUPERSEDED','UNKNOWN'],REVIEWED:['VERIFIED_FOR_SLICE','REJECTED','SUPERSEDED','UNKNOWN'],VERIFIED_FOR_SLICE:['SUPERSEDED'],REJECTED:[],SUPERSEDED:[],UNKNOWN:['AUTHORED_NEW_DRAFT']});
const fail=(code,path)=>Object.freeze({status:'REJECTED',failure:Object.freeze({code,path})});
function validateEnvelope(input,{expectedRef,requiredScope}={}){let x;try{x=structuredClone(input)}catch{return fail('MALFORMED_ENVELOPE','$')}try{if(x.envelopeSchemaVersion!=='1.0.0'||!x.envelopeId||!STATES.includes(x.lifecycleState)||!Array.isArray(x.validationEvidenceRefs)||!Array.isArray(x.reviewEvidenceRefs)||!Array.isArray(x.limitations))return fail('MALFORMED_ENVELOPE','$');x.definitionRef=definitionRef(x.definitionRef);if(expectedRef&&JSON.stringify(x.definitionRef)!==JSON.stringify(expectedRef))return fail('DEFINITION_REF_MISMATCH','definitionRef');if(requiredScope&&x.reviewScope!==requiredScope)return fail('REVIEW_SCOPE_MISMATCH','reviewScope');if(['VALIDATED','REVIEWED','VERIFIED_FOR_SLICE'].includes(x.lifecycleState)&&!x.validationEvidenceRefs.length)return fail('VALIDATION_EVIDENCE_REQUIRED','validationEvidenceRefs');if(['REVIEWED','VERIFIED_FOR_SLICE'].includes(x.lifecycleState)){if(x.reviewDecision!=='APPROVED_FOR_SLICE'||!x.reviewId||!x.reviewEvidenceRefs.length||x.reviewedDefinitionDigest!==x.definitionRef.definitionDigest||x.reviewedDefinitionRevision!==x.definitionRef.revision||!x.reviewScope)return fail('REVIEW_REQUIRED','review')}const expected=digest(x,'envelopeDigest');if(x.envelopeDigest&&x.envelopeDigest!==expected)return fail('ENVELOPE_DIGEST_MISMATCH','envelopeDigest');x.envelopeDigest=expected;return Object.freeze({status:'VALIDATED',envelope:Object.freeze(x),canonicalBytes:canonicalBytes(x).toString('hex')})}catch(e){return fail(e.code||'MALFORMED_ENVELOPE','$')}}
function createDraft({envelopeId,definitionRef:ref,limitations=[]}){return validateEnvelope({envelopeSchemaVersion:'1.0.0',envelopeId,definitionRef:ref,lifecycleState:'AUTHORED_NEW_DRAFT',validationEvidenceRefs:[],reviewEvidenceRefs:[],reviewId:null,reviewDecision:null,reviewedDefinitionDigest:null,reviewedDefinitionRevision:null,reviewScope:null,limitations,supersession:null}).envelope}
function transitionEnvelope(current,next,patch={}){const valid=validateEnvelope(current);if(valid.status!=='VALIDATED')throw Object.assign(Error(valid.failure.code),{code:valid.failure.code});if(!NEXT[current.lifecycleState].includes(next))throw Object.assign(Error('INVALID_LIFECYCLE_TRANSITION'),{code:'INVALID_LIFECYCLE_TRANSITION'});const x={...structuredClone(current),...structuredClone(patch),lifecycleState:next};delete x.envelopeDigest;const out=validateEnvelope(x,{expectedRef:current.definitionRef,requiredScope:patch.requiredScope});if(out.status!=='VALIDATED')throw Object.assign(Error(out.failure.code),{code:out.failure.code});return out.envelope}
module.exports={STATES,NEXT,validateEnvelope,createDraft,transitionEnvelope};
