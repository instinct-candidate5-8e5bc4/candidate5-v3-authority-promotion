'use strict';const {MACHINE_ID}=require('../contracts/semantics');
const TYPES=Object.freeze(['PHYSICAL_BODY','PROFILE','POSTURE']);
function definitionRef({definitionType,definitionId,revision,definitionDigest}){if(!TYPES.includes(definitionType)||!MACHINE_ID.test(definitionId||'')||!Number.isInteger(revision)||revision<1||!/^[a-f0-9]{64}$/.test(definitionDigest||''))throw Object.assign(Error('INVALID_DEFINITION_REF'),{code:'INVALID_DEFINITION_REF'});return Object.freeze({definitionType,definitionId,revision,definitionDigest})}
function refFor(type,definition){if(type==='PHYSICAL_BODY')return definitionRef({definitionType:type,definitionId:definition.bodyDefinitionId,revision:definition.bodyRevision,definitionDigest:definition.canonicalDigest});if(type==='PROFILE')return definitionRef({definitionType:type,definitionId:definition.profileDefinitionId,revision:definition.profileRevision,definitionDigest:definition.profileDigest});if(type==='POSTURE')return definitionRef({definitionType:type,definitionId:definition.postureDefinitionId,revision:definition.postureRevision,definitionDigest:definition.canonicalDigest});throw Object.assign(Error('INVALID_DEFINITION_REF'),{code:'INVALID_DEFINITION_REF'})}
function matches(ref,type,definition){try{return JSON.stringify(refFor(type,definition))===JSON.stringify(ref)}catch{return false}}
module.exports={TYPES,definitionRef,refFor,matches};
