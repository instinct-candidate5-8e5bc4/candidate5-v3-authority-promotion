'use strict';const {PROVENANCE_STATUSES,SUPPORT_SEMANTIC_TYPES,POSTURE_SEMANTIC_TYPES,SUBJECT_SEXES}=require('./constants'),{digest}=require('./canonical');
const MACHINE_ID=/^[a-z0-9][a-z0-9._:/-]*$/;function reject(code,path){return {status:'REJECTED',failure:{code,path}}}
function validateSupportCategory(x){if(!x||!MACHINE_ID.test(x.supportCategoryId||''))return reject('MALFORMED_SUPPORT_CATEGORY','supportCategoryId');if(!SUPPORT_SEMANTIC_TYPES.includes(x.supportSemanticType))return reject('UNKNOWN_SUPPORT_SEMANTIC','supportSemanticType');if(!PROVENANCE_STATUSES.includes(x.provenanceStatus)||typeof x.fixtureOnly!=='boolean')return reject('SEMANTIC_PROVENANCE_CONFUSION','provenanceStatus');if(x.fixtureOnly&&x.provenanceStatus!=='UNKNOWN')return reject('SEMANTIC_PROVENANCE_CONFUSION','fixtureOnly');const y=structuredClone(x);y.categoryDigest=digest(y,'categoryDigest');return {status:'VALIDATED',definition:Object.freeze(y)}}
function validateProfile(x){if(!x||!MACHINE_ID.test(x.profileDefinitionId||'')||!Number.isInteger(x.profileRevision)||x.profileRevision<1)return reject('MALFORMED_PROFILE','$');if(!SUBJECT_SEXES.includes(x.subjectSex))return reject('UNKNOWN_SUBJECT_SEX','subjectSex');if(x.status!=='AUTHORED_NEW_DRAFT')return reject('INVALID_STATUS','status');const y=structuredClone(x);y.profileDigest=digest(y,'profileDigest');return {status:'VALIDATED',definition:Object.freeze(y)}}
function postureSemantic(x){return POSTURE_SEMANTIC_TYPES.includes(x)}module.exports={MACHINE_ID,validateSupportCategory,validateProfile,postureSemantic};
