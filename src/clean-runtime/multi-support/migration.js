'use strict';const {sortRelations}=require('./relations');function migrateLegacyWorld(input,convert){if(input.stateSchemaVersion==='2.0.0')throw Error('ALREADY_V2');if(!input||!input.entities||!Array.isArray(input.physicalRelations))throw Error('AMBIGUOUS_LEGACY_STATE');const relations=[];for(const id of Object.keys(input.entities).sort()){const e=input.entities[id];if(e.supportRelations!==undefined)throw Error('AMBIGUOUS_LEGACY_STATE');if(e.supportRelation){const r=convert(id,structuredClone(e.supportRelation),e);if(!r||r.supportedEntityId!==id)throw Error('AMBIGUOUS_LEGACY_SUPPORT');relations[relations.length]=r}}const entities=structuredClone(input.entities);for(const e of Object.values(entities))delete e.supportRelation;const out={...structuredClone(input),stateSchemaVersion:'2.0.0',entities,supportRelations:sortRelations(relations)};delete out.physicalRelations;delete out.stateDigest;return out}module.exports={migrateLegacyWorld};
