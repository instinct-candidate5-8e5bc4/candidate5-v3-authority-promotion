'use strict';const {digest}=require('./canonical');
const LIFECYCLES=Object.freeze(['INITIALIZING','ACTIVE','PAUSED','CLOSED']);
function deepFreeze(v){if(v&&typeof v==='object'&&!Object.isFrozen(v)){Object.freeze(v);for(const x of Object.values(v))deepFreeze(x)}return v}
function entity(input){if(Object.isFrozen(input))return input;const x=structuredClone(input);for(const k of ['entityId','entityTypeId','revision','lifecycleState','transform','physicalBodyRef','geometrySourceRef','physicalState'])if(x[k]===undefined)throw TypeError('INVALID_ENTITY_STATE:'+k);if(!Number.isInteger(x.revision)||x.revision<1)throw TypeError('INVALID_ENTITY_REVISION');return deepFreeze(x)}
function world(input){const entities={};for(const [id,e] of Object.entries(input.entities||{}))entities[id]=entity(e);const body={worldId:input.worldId,sceneDefinitionRef:input.sceneDefinitionRef||null,revision:input.revision??0,priorStateDigest:input.priorStateDigest||null,lifecycleState:input.lifecycleState||'INITIALIZING',entities,physicalRelations:structuredClone(input.physicalRelations||[]),surfaces:input.surfaces||null,environmentPhysicalState:input.environmentPhysicalState||null,committedEventSequence:input.committedEventSequence||0};if(!body.worldId||!LIFECYCLES.includes(body.lifecycleState))throw TypeError('INVALID_WORLD_STATE');body.stateDigest=digest(body);return deepFreeze(body)}
module.exports={LIFECYCLES,entity,world,deepFreeze};
