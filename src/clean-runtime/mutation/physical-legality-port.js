'use strict';const OUTCOMES=Object.freeze(['PASS','FAIL','UNKNOWN']);function assertPort(p){if(!p||typeof p.evaluate!=='function')throw TypeError('INVALID_PHYSICAL_LEGALITY_PORT');return p}function testDouble(resolver){return Object.freeze({kind:'TEST_DOUBLE_ONLY',evaluate:input=>{const x=resolver(structuredClone(input));if(!x||!OUTCOMES.includes(x.outcome))throw TypeError('INVALID_LEGALITY_OUTCOME');return Object.freeze(structuredClone(x))}})}module.exports={OUTCOMES,assertPort,testDouble};
