'use strict';const crypto=require('node:crypto');function sort(v){if(Array.isArray(v))return v.map(sort);if(v&&typeof v==='object')return Object.fromEntries(Object.keys(v).sort().map(k=>[k,sort(v[k])]));return v;}function canonical(v){return JSON.stringify(sort(v));}function digest(v){return crypto.createHash('sha256').update(canonical(v)).digest('hex');}module.exports={canonical,digest};
