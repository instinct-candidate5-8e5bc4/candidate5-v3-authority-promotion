# Legacy bypass audit plan

Phase 1 records an inventory only. It does not remove, route, disable, or alter legacy paths. A later separately approved phase must reconcile a static AST call graph, runtime physical-mutation log, and PlacementEvidence log exactly. Any active physical mutation without evidence remains `LEGACY_PLACEMENT_BYPASS_ACTIVE`.
