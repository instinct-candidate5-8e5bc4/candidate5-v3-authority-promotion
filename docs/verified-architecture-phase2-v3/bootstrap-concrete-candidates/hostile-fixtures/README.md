# Permanent hostile regressions

Each `VALID_SIGNATURE_WRONG_BINDING` case is a four-artifact independently reviewable signed root: `*.cross.json`, `*.cross.sig`, matching `*.pin.json`, and `*.pin.sig`. The cross record differs from the main accepted record by exactly one advertised binding. Both signatures verify. The fixture pin is the sole canonical pin expected for that fixture cross/signature while retaining the main policy, floor, public-key and offline-manifest bindings. Therefore pin exact comparison passes, and the service reaches cross exact comparison, which MUST fail as `E_CROSS_BINDING` before launcher invocation.
