# certification-vs-rehearsal.diff - NORMALIZED structural diff, NOT raw-byte evidence.
# Produced by: diff -u <(sed 's/OVMF_CI_SECURE_BOOT_UKI/NON_CERTIFYING_REHEARSAL/g' CERT) REHEARSAL
# The sed rewrites certification identity EVERYWHERE, including the OVMF_CI_SECURE_BOOT_UKI_PASS
# marker string and step names. Raw-byte evidence is the unnormalized diff in the #9 packet.
--- /dev/fd/63	2026-09-25 04:41:22.701510968 +0300
+++ ../../../../../.github/workflows/NON_CERTIFYING_REHEARSAL-workflow.yml	2026-09-25 04:40:20.229510968 +0300
@@ -1,34 +1,26 @@
-# NON_CERTIFYING_REHEARSAL — Option A CI certification ceremony workflow.
-# Scope: QEMU/KVM + reproducible DEBUG OVMF Secure Boot certification of the EXACT accepted
-# signed UKI enters via evidence/successor-to-certify.efi (signed-slot head: the owner-signed bytes 0ea8dd7d.. / 21,156,176 B are COMMITTED at the slot, pinned by preflight EXPECT in every lane; this workflow still dies E_SIGNED_UKI_ABSENT if the file ever vanishes).
-# Expected pass = firmware Secure Boot accept + kernel exec + signed-init exit-98
-# (E_PROVIDER_NAMESPACE / E_PROVIDER_LINK_MISSING on non-Google hardware). This is NOT a
-# full runtime PASS, NOT a dm-verity runtime PASS, NOT a provisioning PASS, NOT a Google
-# platform certification, NOT a complete boot-chain certification; NO Google firmware,
-# vTPM, GCP-deployment or persistence claims.
-# Trigger: push ONLY, exact branch filter p3-certification (no wildcards, no other events).
-# Per GitHub Actions documentation a push runs the workflow file AT the pushed commit,
-# including on non-default branches, so this file lives ONLY at .github/workflows/ on the
-# candidate commit (ONE copy; no default-branch registration, no docs/ copy).
-# Ref discipline: the p3-certification ref MUST NOT exist before static acceptance. After
-# STATIC ACCEPT the accepted candidate SHA is pushed ONCE as p3-certification - certified
-# SHA == reviewed SHA. No re-runs, no retries, no additional commits to the ref.
-# Fail-closed: the first step of every job aborts the run unless github.run_attempt == 1
-# AND github.ref is exactly refs/heads/p3-certification. Any accidental run is preserved
-# and reported. Every GitHub action is pinned to a full-length commit SHA (reviewer
-# condition 8; SHAs recorded in the deterministic manifest). bwrap is load-bearing for
-# the canonical build path, so it comes ONLY from the hash-verified platform lock as a
-# staged binary (never the host tool). KVM-only, fail-closed, no TCG. Public standard
-# runner only ($0).
-# D1 conformance: this workflow intentionally KEEPS the rehearsal PREFIX and every
-# /tmp/NON_CERTIFYING_REHEARSAL-* scratch path - the frozen scripts/config hardcode them,
-# so certification evidence is provably produced by the exact rehearsed bytes. ONLY the
-# mode labels, artifact names, trigger/gate, the removed forbidden-marker guard and the
-# closing marker job differ from the rehearsal workflow.
+# NON_CERTIFYING_REHEARSAL — pre-freeze rehearsal workflow (development evidence only).
+# This workflow NEVER certifies anything. It must never emit OVMF_CI_SECURE_BOOT_UKI_PASS
+# or any certification marker. Every artifact name begins NON_CERTIFYING_REHEARSAL.
+# Trigger: push ONLY, exact branch filter p3-rehearsal-3 / p3-rehearsal-4 (no wildcards, no
+# other events). Per GitHub Actions documentation a push runs the workflow file AT the pushed
+# commit, including on non-default branches, so this file lives ONLY at .github/workflows/
+# on the candidate commit (ONE copy; no default-branch registration, no docs/ copy).
+# The two rehearsals are two separate fresh pushes of the SAME candidate SHA: first
+# p3-rehearsal-3, then p3-rehearsal-4 only after -3 passes. No other commits to these refs.
+# (Ref rotation per reviewer ruling after D2: p3-rehearsal-1 is spent at 02f3eab9 and
+# p3-rehearsal-2 was never created; -1/-2 are removed so a stray push to a spent ref
+# can never run.)
+# Fail-closed: the first step of every job aborts the run unless github.run_attempt == 1 AND
+# github.ref is exactly one of the two filtered refs. No re-run counts; any accidental run
+# is preserved and reported. Every GitHub action is pinned to a full-length commit SHA
+# (reviewer condition 8; SHAs recorded in the deterministic manifest). bwrap is
+# load-bearing for the canonical build path, so it comes ONLY from the hash-verified
+# platform lock as a staged binary (never the host tool). KVM-only, fail-closed, no TCG.
+# Public standard runner only ($0).
 name: NON_CERTIFYING_REHEARSAL
 on:
   push:
-    branches: [p3-certification]
+    branches: [p3-rehearsal-3, p3-rehearsal-4]
 permissions: {}
 env:
   # #15 (run-14 verdict): importing the shared fetch_locked helper must never write
@@ -37,10 +29,9 @@
   PREFIX: NON_CERTIFYING_REHEARSAL
   # peer adjudication (1): per-workflow singleton allowlist; scripts fail closed on mismatch.
   ALLOWED_PREFIX: NON_CERTIFYING_REHEARSAL
-  CERTIFICATION_TARGET: "1"
-  # peer run-11 D-e: cert uses the SAME shared-list mechanism - the evidence upload paths
-  # AND the manifest members come from this ONE list (upload step aliases the anchor), so
-  # the two can never drift. Directories expand to their files at manifest time.
+  # peer run-11 single-list: the evidence upload paths AND the manifest members come from
+  # this ONE shared list - the upload step references the same YAML anchor, so the two can
+  # never drift. Directories expand to their files at manifest time.
   EVIDENCE_UPLOAD_PATHS: &evidence_upload_paths |
     /tmp/NON_CERTIFYING_REHEARSAL-out/
     /tmp/NON_CERTIFYING_REHEARSAL-fw-hashes.txt
@@ -52,13 +43,12 @@
     /tmp/NON_CERTIFYING_REHEARSAL-env2-evidence/
     /tmp/NON_CERTIFYING_REHEARSAL-ovmf-env2/basetools-build-pass1.log
 jobs:
-  certification:
+  rehearsal:
     # ubuntu-22.04 pinned: the canonical-path mechanism (build-ovmf-debug.sh) needs
     # unprivileged user namespaces for bwrap; ubuntu-24.04 runners restrict them via
     # AppArmor.
     runs-on: ubuntu-22.04
     timeout-minutes: 360
-    environment: p3-certification
     steps:
       - name: NON_CERTIFYING_REHEARSAL ref+attempt fail-closed gate
         env:
@@ -68,7 +58,10 @@
         run: |
           set -euo pipefail
           [ "${{ github.run_attempt }}" = "1" ] || { echo "E_RERUN_FORBIDDEN run_attempt=${{ github.run_attempt }}"; exit 89; }
-          [ "${{ github.ref }}" = "refs/heads/p3-certification" ] || { echo "E_REF_MISMATCH ref=${{ github.ref }}"; exit 89; }
+          case "${{ github.ref }}" in
+            refs/heads/p3-rehearsal-3|refs/heads/p3-rehearsal-4) : ;;
+            *) echo "E_REF_MISMATCH ref=${{ github.ref }}"; exit 89 ;;
+          esac
           # pushed-once gate (T3 F5): the ref must be created by THIS push, never force-pushed
           [ "$EV_CREATED" = "true" ] || { echo "E_PUSHED_ONCE created=$EV_CREATED"; exit 89; }
           [ "$EV_FORCED" = "false" ] || { echo "E_PUSHED_ONCE forced=$EV_FORCED"; exit 89; }
@@ -87,8 +80,7 @@
           # D2 gate (reviewer ruling): every relative file/script argument used by later
           # steps must exist in the committed tree. Fixed list, fail-closed exit 88.
           cd docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
-          [ -e evidence/successor-to-certify.efi ] || { echo "E_SIGNED_UKI_ABSENT evidence/successor-to-certify.efi (post-signing state: the owner-signed UKI is committed and hash-pinned; this fires only if the file vanishes - no fallback)"; exit 90; }
-          for f in             platform.lock.json             config.json             stage-platform.sh             make-shims.sh             preflight-check.py             build-ovmf-debug.sh             scan-pe-pdb-paths.py             build-enroll-app.sh             bwrap-argv.sh             enroll-predicate-check.py             rehearsal-enroll.sh             ../build-esp-image.sh             run-ceremony.sh             qemu-smoke.sh; do
+          for f in             platform.lock.json             config.json             stage-platform.sh             make-shims.sh             test-c-sign-confinement.py             test-enroll-predicate-env.py             test-cert-positive-guard.py             test-k2-sweeps.py             test-inrun-writes.py             test-inrun-stale-path.py             test-inrun-allowlist.py             test-stale-state-committed.py             test-heredoc-structure.py             test-h5-planted-errset.py             preflight-check.py             build-ovmf-debug.sh             scan-pe-pdb-paths.py             build-enroll-app.sh             bwrap-argv.sh             enroll-predicate-check.py             rehearsal-enroll.sh             ../build-esp-image.sh             evidence/successor-signed.efi             evidence/successor-to-certify.efi             run-ceremony.sh             qemu-smoke.sh             config_schema.py             bind-fixture-certs.sh             test-config-schema.py             test-fixture-cert-binding.py             test-env-contract.py             test-hist-esp-guard.py             test-esp-fresh-dirs.py             test-workdir-sibling.py             test-h3-precheck.py             test-argv-freeze.py             test-marker-absence.py             fixtures/run-36037280674-R1-argv.txt             fixtures/run-36042868066-C-ossl-debug-argv.txt; do
             [ -e "$f" ] || { echo "E_WORKFLOW_ARG_MISSING $f"; exit 88; }
           done
       - name: NON_CERTIFYING_REHEARSAL host inputs declared (B3)
@@ -188,6 +180,38 @@
           set -euo pipefail
           cd docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
           python3 preflight-check.py config.json /tmp/$PREFIX-stage
+      - name: NON_CERTIFYING_REHEARSAL c-sign derivation/confinement negative tests (must-show)
+        if: ${{ !cancelled() }}
+        run: |
+          set -eEuo pipefail
+          trap '_rc=$?; echo "E_BASH_ERRTRAP csign-negtest line $LINENO rc=$_rc cmd: $BASH_COMMAND" >&2; exit 97' ERR
+          cd docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
+          # peer 2026-09-24 B1 condition 5: the planted negatives are committed and
+          # runnable; this log lands in the run evidence tree.
+          mkdir -p /tmp/$PREFIX-out
+          python3 test-c-sign-confinement.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-csign-negtests.log
+          python3 test-enroll-predicate-env.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-predicate-env-negtests.log
+          python3 test-cert-positive-guard.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-certpos-guard-negtests.log
+          python3 test-k2-sweeps.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-k2-sweeps-negtests.log
+          python3 test-stale-state-committed.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-stale-state-negtests.log
+          python3 test-heredoc-structure.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-heredoc-structure-negtests.log
+          python3 test-h5-planted-errset.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-h5-planted-errset-tests.log
+          python3 test-inrun-writes.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-inrun-writes-negtests.log
+          python3 test-inrun-stale-path.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-inrun-stale-path-negtests.log
+          python3 test-inrun-allowlist.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-inrun-allowlist-negtests.log
+          # peer run-36017957182 ruling: schema / fixture-cert-binding / env-contract suites
+          python3 test-config-schema.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-config-schema-tests.log
+          python3 test-fixture-cert-binding.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-fixture-cert-binding-tests.log
+          python3 test-env-contract.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-env-contract-tests.log
+          # peer run-36024634796 ruling: historical-ESP boot-expectation guard + workdir sibling-closure gate
+          python3 test-hist-esp-guard.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-hist-esp-guard-tests.log
+          python3 test-esp-fresh-dirs.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-esp-fresh-dirs-tests.log
+          python3 test-workdir-sibling.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-workdir-sibling-tests.log
+          # peer run-36031372949 ruling: H3 precheck taxonomy, executed-argv freeze contract, marker-at-rest
+          python3 test-h3-precheck.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-h3-precheck-tests.log
+          python3 test-argv-freeze.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-argv-freeze-tests.log
+          python3 test-marker-absence.py 2>&1 | tee /tmp/$PREFIX-out/$PREFIX-marker-absence-tests.log
+
       - name: NON_CERTIFYING_REHEARSAL reproducible OVMF DEBUG dual build + accepted-firmware pins
         timeout-minutes: 150
         run: |
@@ -236,6 +260,7 @@
       - name: NON_CERTIFYING_REHEARSAL network-cut canary (must fail)
         run: |
           set -euo pipefail
+          # batch1r3 C3: a fetch inside the cut namespace MUST fail, proving the cut is real
           if sudo unshare -n curl -fsS --max-time 10 https://archive.ubuntu.com/ >/dev/null 2>&1; then
             echo "E_NET_CANARY_LEAK: network reachable inside unshare -n"; exit 96
           fi
@@ -245,18 +270,40 @@
         run: |
           set -euo pipefail
           cd docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
+          # batch1r3 C3: both builders run with the network enforced off; all inputs are staged
           sudo unshare -n ./build-enroll-app.sh /tmp/$PREFIX-stage /tmp/$PREFIX-app-a /tmp/$PREFIX-app-b
           cmp /tmp/$PREFIX-app-a/enroll-app.efi /tmp/$PREFIX-app-b/enroll-app.efi
           sha256sum /tmp/$PREFIX-app-a/enroll-app.efi >> /tmp/$PREFIX-fw-hashes.txt
           # batch1r3 C1: the ESP step runs with the staged-loader shims on PATH
           export PATH="/tmp/$PREFIX-stage/shims:$PATH"
-          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-to-certify.efi /tmp/$PREFIX-esp-a 0ea8dd7da95eece8fe8eefed9f3fb701b9cdfd09abe5cfdd5f1954dd0d2f6959 21156176 c5-successor-to-certify-esp.raw
-          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-to-certify.efi /tmp/$PREFIX-esp-b 0ea8dd7da95eece8fe8eefed9f3fb701b9cdfd09abe5cfdd5f1954dd0d2f6959 21156176 c5-successor-to-certify-esp.raw
-          [ "$(sha256sum /tmp/$PREFIX-esp-a/c5-successor-to-certify-esp.raw | cut -d' ' -f1)" = \
-            "6ff0d9ef89668ef79e60eba41eb70704664008697637797b384e6e67075d2f77" ]
-          cmp /tmp/$PREFIX-esp-a/c5-successor-to-certify-esp.raw /tmp/$PREFIX-esp-b/c5-successor-to-certify-esp.raw
+          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-signed.efi /tmp/$PREFIX-esp-a 133096976ee70a8c272cbcc8c28d369bfc65d93d994cbff4174b947df00239d1 21166416 c5-root-admitter-uki-v3-esp.raw
+          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-signed.efi /tmp/$PREFIX-esp-b 133096976ee70a8c272cbcc8c28d369bfc65d93d994cbff4174b947df00239d1 21166416 c5-root-admitter-uki-v3-esp.raw
+          [ "$(sha256sum /tmp/$PREFIX-esp-a/c5-root-admitter-uki-v3-esp.raw | cut -d' ' -f1)" = \
+            "2bfa621238890890765c9636f99dabfb46233bfa626afb580ce33e65858019c9" ]
+          cmp /tmp/$PREFIX-esp-a/c5-root-admitter-uki-v3-esp.raw /tmp/$PREFIX-esp-b/c5-root-admitter-uki-v3-esp.raw
           # peer gap close: the ESP is a frozen output; record it in fw-hashes for the
           # env2 cross-runner comparison, and log the artifact digest tied to this run.
+          sha256sum /tmp/$PREFIX-esp-a/c5-root-admitter-uki-v3-esp.raw >> /tmp/$PREFIX-fw-hashes.txt
+          # peer pre-push ruling on 63dc0ed9 (revision 2): the scratch/rehearsal lanes must
+          # dual-build the SLOT ESP from the committed signed slot at pin 6ff0d9ef..,
+          # alongside the historical ESP, so the P1/R7 signed-file cases boot the signed
+          # file's ESP in these lanes. The CI pin gate proves the pin the reviewer could
+          # not reproduce locally. peer run-36081199933 ruling (item 1, allowed
+          # alternative): the slot builds run in SEPARATE FRESH dirs (the builder's
+          # E_WORK_EXISTS guard stays at full strength - reusing esp-a killed run
+          # 36081199933), then the a-product is copied into the esp-a staging dir the
+          # ceremony and qemu-smoke read, closed by a named post-copy pin check.
+          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-to-certify.efi /tmp/$PREFIX-esp-slot-a 0ea8dd7da95eece8fe8eefed9f3fb701b9cdfd09abe5cfdd5f1954dd0d2f6959 21156176 c5-successor-to-certify-esp.raw
+          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-to-certify.efi /tmp/$PREFIX-esp-slot-b 0ea8dd7da95eece8fe8eefed9f3fb701b9cdfd09abe5cfdd5f1954dd0d2f6959 21156176 c5-successor-to-certify-esp.raw
+          [ "$(sha256sum /tmp/$PREFIX-esp-slot-a/c5-successor-to-certify-esp.raw | cut -d' ' -f1)" = \
+            "6ff0d9ef89668ef79e60eba41eb70704664008697637797b384e6e67075d2f77" ]
+          cmp /tmp/$PREFIX-esp-slot-a/c5-successor-to-certify-esp.raw /tmp/$PREFIX-esp-slot-b/c5-successor-to-certify-esp.raw
+          # peer run-36081199933-review ruling (privilege mismatch): the copy target dir was
+          # created by the SUDO builder (root:root 0755) - the copy must run under sudo too,
+          # or it dies EACCES before the pin check can fire.
+          sudo cp --sparse=always /tmp/$PREFIX-esp-slot-a/c5-successor-to-certify-esp.raw /tmp/$PREFIX-esp-a/c5-successor-to-certify-esp.raw
+          [ "$(sha256sum /tmp/$PREFIX-esp-a/c5-successor-to-certify-esp.raw | cut -d' ' -f1)" = \
+            "6ff0d9ef89668ef79e60eba41eb70704664008697637797b384e6e67075d2f77" ] || { echo "E_SLOT_ESP_COPY_PIN /tmp/$PREFIX-esp-a/c5-successor-to-certify-esp.raw"; exit 46; }
           sha256sum /tmp/$PREFIX-esp-a/c5-successor-to-certify-esp.raw >> /tmp/$PREFIX-fw-hashes.txt
           echo "ARTIFACT-DIGEST path=/tmp/$PREFIX-fw-hashes.txt sha256=$(sha256sum /tmp/$PREFIX-fw-hashes.txt | cut -d' ' -f1) run_id=$GITHUB_RUN_ID run_attempt=$GITHUB_RUN_ATTEMPT run_number=$GITHUB_RUN_NUMBER workflow=$GITHUB_WORKFLOW commit=$GITHUB_SHA ref=$GITHUB_REF"
       - name: NON_CERTIFYING_REHEARSAL unsigned UKI in-run build (reviewed gapless builder)
@@ -329,7 +376,126 @@
           cmp "$G/signer.der" rehearsal/evidence/c5-signing-cert.der || { echo "E_SLOT_SIGNER_CMP signer != committed c5-signing-cert.der (7cda4ddc..)"; exit 91; }
           echo "signed-slot committed-bytes gates PASS: delta+signed+authenticode+signer-cmp (slot $SH, signer $(sha256sum "$G/signer.der" | cut -d' ' -f1))"
           SLOTGATES
-      - name: NON_CERTIFYING_REHEARSAL fixture generation (in-run ephemeral keys, no c-sign)
+      - name: NON_CERTIFYING_REHEARSAL criterion-C throwaway signing (c-sign, ephemeral in-run key)
+        if: ${{ !cancelled() }}
+        timeout-minutes: 20
+        run: |
+          set -eEuo pipefail
+          trap '_rc=$?; echo "E_BASH_ERRTRAP c-sign line $LINENO rc=$_rc cmd: $BASH_COMMAND" >&2; exit 97' ERR
+          cd docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
+          export PATH="/tmp/$PREFIX-stage/shims:$PATH"
+          [ -x "/tmp/$PREFIX-stage/shims/osslsigncode" ] || { echo "E_TOOL_MISSING osslsigncode shim"; exit 92; }
+          [ -x "/tmp/$PREFIX-stage/shims/sbsign" ] || { echo "E_TOOL_MISSING sbsign shim"; exit 92; }
+          [ -x "/tmp/$PREFIX-stage/shims/openssl" ] || { echo "E_TOOL_MISSING openssl shim"; exit 92; }
+          C=/tmp/$PREFIX-inrun/c-sign
+          rm -rf "$C"; mkdir -p "$C"
+          # network-enforced-off like every other post-staging build step (batch1r3 C3);
+          # the inner body runs as root solely for the namespace, then ownership is repaired.
+          sudo unshare -n env PATH="$PATH" PREFIX="$PREFIX" C="$C" bash -se <<'CSIGN'
+          set -eEuo pipefail
+          trap '_rc=$?; echo "E_BASH_ERRTRAP c-sign-inner line $LINENO rc=$_rc cmd: $BASH_COMMAND" >&2; exit 97' ERR
+          # zero-private-key rule: the ephemeral signing key exists ONLY at
+          # /tmp/$PREFIX-csign-key/ (mode 0700, outside the checkout and every upload
+          # path) and is shred -u'd by an in-step shell trap on EVERY exit path,
+          # including job cancellation (peer hardening note: on cancellation only in-step
+          # traps are guaranteed; never an always() cleanup step), plus an explicit
+          # shred -u after the gates. The key is never committed, never uploaded,
+          # never printed.
+          K=/tmp/$PREFIX-csign-key
+          mkdir -p "$K"; chmod 0700 "$K"
+          # run-36014385477 ruling (a): public-cert PEM intermediates NEVER land in $INRUN -
+          # they live in this sibling 0700 transient dir and are shred -u'd by the same EXIT
+          # trap and explicit cleanup as the key, with residue checks. $INRUN holds .der ONLY.
+          P=/tmp/$PREFIX-csign-pem
+          mkdir -p "$P"; chmod 0700 "$P"
+          trap 'shred -u "$K/throwaway-key.pem" "$P/c5-throwaway-ci-cert.pem" "$P/signer-ossl.pem" "$P/signer-sbsign.pem" 2>/dev/null || true; rmdir "$K" "$P" 2>/dev/null || true' EXIT
+          U=/tmp/$PREFIX-inrun/uki/successor-unsigned.efi
+          got=$(sha256sum "$U" | cut -d' ' -f1)
+          [ "$got" = "4cda9c3e285b5b639364234400bf0b121178f12447cc88d896cd2623e07d18e1" ] || { echo "E_UNSIGNED_UKI_DRIFT got=$got"; exit 91; }
+          openssl req -x509 -newkey rsa:2048 -keyout "$K/throwaway-key.pem" -out "$P/c5-throwaway-ci-cert.pem" -days 1 -nodes -subj "/CN=C5-THROWAWAY-CI-SIGNER/" -sha256
+          openssl x509 -in "$P/c5-throwaway-ci-cert.pem" -outform DER -out "$C/c5-throwaway-ci-cert.der"
+          # arm 1: osslsigncode (PE Authenticode attach); arm 2: sbsign
+          osslsigncode sign -certs "$P/c5-throwaway-ci-cert.pem" -key "$K/throwaway-key.pem" -h sha256 -in "$U" -out "$C/signed-ossl.efi"
+          sbsign --key "$K/throwaway-key.pem" --cert "$P/c5-throwaway-ci-cert.pem" --out "$C/signed-sbsign.efi" "$U"
+          # per-arm in-job gates: (a) embedded digest == recomputed section-wise ==
+          # b2f655b0 == the pinned unsigned artifact's dual-equal digest; (b) signed
+          # delta == ONLY the security directory + CheckSum under the refined tail rule;
+          # (c) extracted signer cert == THIS run's throwaway cert.
+          for v in ossl sbsign; do
+            python3 ../verify-uki-signed.py "$C/signed-$v.efi" | tee "$C/gate4b-$v.json"
+            python3 - "$C/gate4b-$v.json" <<'PYG4B'
+          import json, sys
+          r = json.load(open(sys.argv[1]))
+          assert r["result"] == "PASS", r
+          want = "b2f655b01cb1587500e163916be8f10fe1eb10f11905597b76cdc51e088924de"
+          if r["embedded_digestinfo"] != want or r["sectionwise"] != want:
+              print("E_UKI_FIRMWARE_DIGEST_MISMATCH", r["embedded_digestinfo"], r["sectionwise"]); sys.exit(91)
+          PYG4B
+            python3 ../verify-signed-delta.py "$U" "$C/signed-$v.efi" | tee "$C/delta-$v.json"
+            python3 - "$C/signed-$v.efi" "$C/pkcs7-$v.der" <<'PYX'
+          import struct, sys
+          b = open(sys.argv[1], "rb").read()
+          pe = struct.unpack_from("<I", b, 0x3C)[0]
+          # security directory = data-directory index 4: PE32+ optional-header offset 144
+          va, sz = struct.unpack_from("<II", b, pe + 24 + 144)
+          open(sys.argv[2], "wb").write(b[va+8:va+sz])
+          PYX
+            python3 ../verify-uki-authenticode.py "$C/pkcs7-$v.der" | tee "$C/auth-$v.json"
+            openssl pkcs7 -inform DER -in "$C/pkcs7-$v.der" -print_certs -out "$P/signer-$v.pem"
+            openssl x509 -in "$P/signer-$v.pem" -outform DER -out "$C/signer-$v.der"
+            cmp "$C/signer-$v.der" "$C/c5-throwaway-ci-cert.der" || { echo "E_SIGNER_CERT_MISMATCH $v"; exit 91; }
+          done
+          ( cd "$C" && sha256sum signed-ossl.efi signed-sbsign.efi c5-throwaway-ci-cert.der > SHASUMS )
+          cat "$C/SHASUMS"
+          shred -u "$K/throwaway-key.pem" "$P/c5-throwaway-ci-cert.pem" "$P/signer-ossl.pem" "$P/signer-sbsign.pem"
+          [ ! -e "$K/throwaway-key.pem" ] || { echo "E_KEY_RESIDUE $K/throwaway-key.pem"; exit 95; }
+          for pem in "$P/c5-throwaway-ci-cert.pem" "$P/signer-ossl.pem" "$P/signer-sbsign.pem"; do
+            [ ! -e "$pem" ] || { echo "E_PEM_RESIDUE $pem"; exit 95; }
+          done
+          rmdir "$K"
+          rmdir "$P"
+          # K2 over the in-run product tree AND the checkout build-output tree
+          # (peer run-36009604654 ruling (d)): no key-named file and no PEM/DER
+          # private-key content under /tmp/$PREFIX-inrun or (if present) build-output.
+          python3 - <<'PYK2C'
+          import os, sys, re
+          def _raise(e):
+              print("E_K2_SWEEP_UNREADABLE %s" % e); sys.exit(95)
+          roots = ["/tmp/%s-inrun" % os.environ["PREFIX"]]
+          if os.path.isdir("build-output"):
+              roots.append("build-output")
+          # run-36014385477 ruling (b): SAME name set as the zero-private-key gates
+          # (.key/.pem/.p12/.pfx extension PLUS the 'key' substring) and SAME content
+          # patterns (PEM "PRIVATE KEY" + PKCS#8/RSA DER key headers) - this class fails
+          # HERE inside c-sign, not three steps later at the upload gate.
+          name_re = re.compile(r"\.(key|pem|p12|pfx)$", re.I)
+          der_rsa = re.compile(rb"\x30\x82..\x02\x01\x00\x02\x82", re.S)
+          der_p8 = re.compile(rb"\x30\x82..\x02\x01\x00\x30", re.S)
+          bad = []; scanned = 0
+          for path in roots:
+              for root, dirs, files in os.walk(path, onerror=_raise):
+                  for f in files:
+                      scanned += 1
+                      fp = os.path.join(root, f)
+                      if name_re.search(f) or "key" in f.lower():
+                          bad.append(fp + " (key-suspect name)")
+                          continue
+                      with open(fp, "rb") as h:
+                          d = h.read()
+                      if b"PRIVATE KEY" in d or der_rsa.search(d) or der_p8.search(d):
+                          bad.append(fp + " (private-key content)")
+          if bad:
+              print("E_PRIVATE_KEY_IN_BUILD_TREE", bad); sys.exit(95)
+          if scanned == 0:
+              print("E_K2_SWEEP_EMPTY %s" % roots); sys.exit(95)
+          print("K2 c-sign/inrun sweep clean (scanned=%d roots=%s)" % (scanned, roots))
+          PYK2C
+          echo "c-sign complete: two arms gated, key shredded outside the checkout, SHASUMS recorded"
+          CSIGN
+          sudo chown -R "$(id -u):$(id -g)" "$C"
+          [ ! -d "/tmp/$PREFIX-csign-key" ] || { echo "E_KEY_RESIDUE /tmp/$PREFIX-csign-key"; exit 95; }
+          [ ! -d "/tmp/$PREFIX-csign-pem" ] || { echo "E_PEM_RESIDUE /tmp/$PREFIX-csign-pem"; exit 95; }
+      - name: NON_CERTIFYING_REHEARSAL criterion-C fixture generation (in-run ephemeral keys)
         if: ${{ !cancelled() }}
         timeout-minutes: 20
         run: |
@@ -339,12 +505,7 @@
           export PATH="/tmp/$PREFIX-stage/shims:$PATH"
           [ -x "/tmp/$PREFIX-stage/root/usr/bin/sbsign" ] || { echo "E_TOOL_MISSING staged sbsign"; exit 92; }
           [ -x "/tmp/$PREFIX-stage/root/usr/bin/sbverify" ] || { echo "E_TOOL_MISSING staged sbverify"; exit 92; }
-          # peer run-36048129342 signed-slot ruling (f): fixture generation runs with NO
-          # c-sign/throwaway step in this lane; it consumes ONLY the in-run unsigned UKI from
-          # the preceding build step, and produces run-ceremony.sh's required paths
-          # ($INRUN/uki/successor-unsigned.efi, $INRUN/c-sign/fixtures + FIXTURE-SHASUMS).
-          # The throwaway signing record is absent by design; nothing may require it.
-          [ -f "/tmp/$PREFIX-inrun/uki/successor-unsigned.efi" ] || { echo "E_UNSIGNED_UKI_MISSING /tmp/$PREFIX-inrun/uki/successor-unsigned.efi"; exit 94; }
+          [ -f "/tmp/$PREFIX-inrun/c-sign/SHASUMS" ] || { echo "E_CSIGN_RECORD_MISSING /tmp/$PREFIX-inrun/c-sign/SHASUMS"; exit 94; }
           # peer 2026-09-24 route-(ii) key lifecycle: fixtures are generated network-off
           # under /tmp/$PREFIX-*; private keys exist only inside fixture-generate.sh's own
           # mktemp workdir and are shred -u'd by its EXIT trap and explicit cleanup;
@@ -403,8 +564,15 @@
           OWNER = "7cda4ddc149849cc61191d4b5b3d218d14770c0401e9e66dd9617b82ba1ae441"
           H = sha(fx + "/C5-HOSTILE-FIXTURE.cer")
           W = sha(fx + "/C5-WRONG-SIGNER-FIXTURE.cer")
+          T = None
+          for line in open(inrun + "/c-sign/SHASUMS"):
+              parts = line.split()
+              if len(parts) == 2 and parts[1] == "c5-throwaway-ci-cert.der":
+                  T = parts[0]
+          if not T:
+              print("E_FIXTURE_RECORD c5-throwaway-ci-cert.der missing from c-sign SHASUMS"); sys.exit(94)
           seen = {}
-          for label, v in (("H", H), ("W", W), ("owner", OWNER)):
+          for label, v in (("H", H), ("W", W), ("T", T), ("owner", OWNER)):
               if v in seen:
                   print("E_FIXTURE_CERT_DISTINCT %s DER sha256 collides with %s (%s)" % (label, seen[v], v)); sys.exit(91)
               seen[v] = label
@@ -422,6 +590,7 @@
               "unsigned_uki_sha256": U,
               "hostile_cert_der_sha256": H,
               "wrong_signer_cert_der_sha256": W,
+              "throwaway_cert_der_sha256": T,
               "tool_versions": {"openssl": ver(["openssl", "version"]),
                                 "sbsign": ver(["/tmp/" + __import__("os").environ["PREFIX"] + "-stage/root/usr/bin/sbsign", "--version"])},
           }
@@ -430,7 +599,7 @@
           shas = sp.run(["sha256sum", "F-WRONGSIG.efi", "F-HOSTILEUKI.efi", "C5-WRONG-SIGNER-FIXTURE.cer", "C5-HOSTILE-FIXTURE.cer"],
                         capture_output=True, text=True, cwd=fx, check=True).stdout
           open(fx + "/FIXTURE-SHASUMS", "w").write(shas)
-          print("GENERATION-RECORD.json + FIXTURE-SHASUMS written; H/W/owner all distinct")
+          print("GENERATION-RECORD.json + FIXTURE-SHASUMS written; H/W/T/owner all distinct")
           PREC
           # K2 after fixture copy-out (peer run-36009604654 ruling (d)): fixtures are
           # public-only; no key-named file and no private-key content under
@@ -467,15 +636,21 @@
               print("E_K2_SWEEP_EMPTY %s" % roots); sys.exit(95)
           print("K2 inrun sweep clean (post fixture copy-out, scanned=%d roots=%s)" % (scanned, roots))
           PYK2F
-          # run-36014385477 ruling (c), adapted for this lane per run-36048129342 ruling (f):
-          # positive allowlist - at the END of the fixture step /tmp/$PREFIX-inrun must contain
-          # EXACTLY this file set (the rehearsal set MINUS every c-sign/throwaway output, listed
-          # in R3 section 30); any extra OR missing file fails E_INRUN_UNEXPECTED_FILE.
+          # run-36014385477 ruling (c): positive allowlist - at the END of the fixture step
+          # /tmp/$PREFIX-inrun must contain EXACTLY this file set (listed identically in
+          # R3 section 23); any extra OR missing file fails E_INRUN_UNEXPECTED_FILE.
           python3 - <<'PYALLOW'
           import os, sys
           base = "/tmp/%s-inrun" % os.environ["PREFIX"]
           want = {
               "uki/successor-unsigned.efi",
+              "c-sign/signed-ossl.efi", "c-sign/signed-sbsign.efi",
+              "c-sign/c5-throwaway-ci-cert.der", "c-sign/signer-ossl.der", "c-sign/signer-sbsign.der",
+              "c-sign/pkcs7-ossl.der", "c-sign/pkcs7-sbsign.der",
+              "c-sign/gate4b-ossl.json", "c-sign/gate4b-sbsign.json",
+              "c-sign/delta-ossl.json", "c-sign/delta-sbsign.json",
+              "c-sign/auth-ossl.json", "c-sign/auth-sbsign.json",
+              "c-sign/SHASUMS",
               "c-sign/fixtures/F-WRONGSIG.efi", "c-sign/fixtures/F-HOSTILEUKI.efi",
               "c-sign/fixtures/C5-WRONG-SIGNER-FIXTURE.cer", "c-sign/fixtures/C5-HOSTILE-FIXTURE.cer",
               "c-sign/fixtures/GENERATION-RECORD.json", "c-sign/fixtures/FIXTURE-SHASUMS",
@@ -499,7 +674,7 @@
           # K2 (peer run-12 K4 containment): fail closed if any private-key material stands
           # under ANY evidence upload path - by NAME (*.key/*.pem/*.p12/*.pfx) or CONTENT
           # (PEM "PRIVATE KEY" markers, PKCS#8/RSA DER key headers). Upload only if this passed.
-          printf '%s\n' "$SCAN_PATHS" > /tmp/$PREFIX-k2-paths.txt
+          printf '%s\n' "$SCAN_PATHS" "/tmp/$PREFIX-inrun" > /tmp/$PREFIX-k2-paths.txt
           python3 - /tmp/$PREFIX-k2-paths.txt <<'PYK'
           import sys, os, re
           paths = [l.strip() for l in open(sys.argv[1]) if l.strip()]
@@ -619,7 +794,7 @@
           [ "$A4" = "$B4" ] || { echo "E_SMOKE_PREP_DRIFT"; exit 97; }
           git ls-files -s -- qemu-smoke.sh bwrap-argv.sh
           git status --porcelain | grep . && { echo "E_SMOKE_PREP_TREE_DIRTY"; git status --porcelain; exit 97; } || echo "git status --porcelain: EMPTY (after repair)"
-          sudo env PREFIX="$PREFIX" ALLOWED_PREFIX="$ALLOWED_PREFIX" ./qemu-smoke.sh config.json argv-freeze.json 7 \
+          sudo env PREFIX="$PREFIX" ALLOWED_PREFIX="$ALLOWED_PREFIX" ./qemu-smoke.sh config.json argv-freeze.json 0 \
               > /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-qemu-smoke.log 2>&1 \
             || { echo "E_QEMU_SMOKE"; cat /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-qemu-smoke.log; exit 97; }
           # peer N8 (log-or-fail): the raw/executed equivalence proof must be IN the durable
@@ -631,13 +806,14 @@
           grep -F "lane argv check:" /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-qemu-smoke.log
           echo "masked-host binding smoke OK (exact frozen case argv + -S, four host qemu dirs absent, no case booted)"
       - name: NON_CERTIFYING_REHEARSAL ceremony (network-enforced-off, tee'd log)
+        id: ceremony
         timeout-minutes: 120
         run: |
           set -euo pipefail
           cd docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
           mkdir -p /tmp/$PREFIX-out
           date +%s >> /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-step-stamps.txt
-          sudo unshare -n env PREFIX="$PREFIX" ALLOWED_PREFIX="$ALLOWED_PREFIX" CERTIFICATION_TARGET="$CERTIFICATION_TARGET" ./run-ceremony.sh config.json /tmp/$PREFIX-stage /tmp/$PREFIX-out 2>&1 \
+          sudo unshare -n env PREFIX="$PREFIX" ALLOWED_PREFIX="$ALLOWED_PREFIX" ./run-ceremony.sh config.json /tmp/$PREFIX-stage /tmp/$PREFIX-out 2>&1 \
             | tee /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-ceremony.log
       - name: NON_CERTIFYING_REHEARSAL evidence readability repair (ephemeral out tree only)
         if: always()
@@ -671,9 +847,10 @@
             echo "E_EVIDENCE_NONREGULAR non-regular file in evidence tree:"; find /tmp/$PREFIX-out ! -type f ! -type d -print; exit 94
           fi
           echo "evidence socket sweep: only regular files+dirs under /tmp/$PREFIX-out"
-          # peer N9 + run-11 D-e single-list: the manifest covers EXACTLY the uploaded
-          # paths - both iterate the ONE shared EVIDENCE_UPLOAD_PATHS list (workflow env
-          # anchor; the upload step aliases the same anchor). Directories expand to their
+          # peer N9 + run-11 single-list: the manifest covers EXACTLY the uploaded paths -
+          # both iterate the ONE shared EVIDENCE_UPLOAD_PATHS list (YAML anchor at the
+          # workflow env block; the upload step aliases the same anchor), so drift between
+          # upload and manifest is impossible by construction. Directories expand to their
           # files; absent members (failed-early runs) are noted, never silently skipped.
           man=$(mktemp)
           { while IFS= read -r p; do
@@ -690,14 +867,45 @@
           sudo chown "$(id -u):$(id -g)" /tmp/$PREFIX-out/$PREFIX-evidence-manifest.txt
           echo "ARTIFACT-DIGEST paths=EVIDENCE_UPLOAD_PATHS(shared-list) manifest=$PREFIX-evidence-manifest.txt manifest_sha256=$(sha256sum /tmp/$PREFIX-out/$PREFIX-evidence-manifest.txt | cut -d' ' -f1) files=$(grep -c . /tmp/$PREFIX-out/$PREFIX-evidence-manifest.txt) run_id=$GITHUB_RUN_ID run_attempt=$GITHUB_RUN_ATTEMPT run_number=$GITHUB_RUN_NUMBER workflow=$GITHUB_WORKFLOW commit=$GITHUB_SHA ref=$GITHUB_REF"
       - name: NON_CERTIFYING_REHEARSAL ceremony interpreter cross-check (addendum2 condition 2b)
+        id: xcheck
         run: |
           set -euo pipefail
           grep '^host-python ' /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-host-inputs.txt > /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-expected-ceremony-python.txt
           diff /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-expected-ceremony-python.txt /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-ceremony-python.txt || { echo "E_CEREMONY_PYTHON_DRIFT"; exit 51; }
           echo "ceremony interpreter identical to the job record"
-      - name: NON_CERTIFYING_REHEARSAL top-level evidence prefix guard (D3)
+      - name: NON_CERTIFYING_REHEARSAL cross-check strictness (peer run-12 C4-addendum)
+        if: success()
+        env:
+          CEREMONY_OUTCOME: ${{ steps.ceremony.outcome }}
+          XCHECK_OUTCOME: ${{ steps.xcheck.outcome }}
         run: |
           set -euo pipefail
+          # a green ceremony MUST have been followed by a completed interpreter cross-check
+          if [ "$CEREMONY_OUTCOME" = "success" ] && [ "$XCHECK_OUTCOME" != "success" ]; then
+            echo "E_XCHECK_SKIPPED green ceremony without a completed interpreter cross-check"; exit 97
+          fi
+          echo "cross-check strictness: ceremony=$CEREMONY_OUTCOME xcheck=$XCHECK_OUTCOME"
+      - name: NON_CERTIFYING_REHEARSAL forbidden-marker guard
+        if: '!cancelled()'
+        run: |
+          set -euo pipefail
+          # C4 stand-down: a run cancelled before staging has no evidence tree to scan
+          if [ ! -d /tmp/$PREFIX-out ]; then
+            echo "marker guard stand-down: evidence tree absent (cancelled before staging)"; exit 0
+          fi
+          # fail-closed form: explicit if, fixed-string binary-inclusive scan (-a -F) of the
+          # ENTIRE evidence tree (which includes the tee'd ceremony log), then a top-level
+          # prefix guard so no un-prefixed path can sit next to the prefixed evidence.
+          set +e
+          GUARD_OUT=$(grep -r -a -F -l "OVMF_CI_SECURE_BOOT_UKI_PASS" /tmp/$PREFIX-out)
+          GUARD_RC=$?
+          set -e
+          if [ "$GUARD_RC" -eq 0 ]; then
+            echo "E_FORBIDDEN_MARKER_IN_EVIDENCE"; printf '%s\n' "$GUARD_OUT"; exit 94
+          fi
+          if [ "$GUARD_RC" -ne 1 ]; then
+            echo "E_MARKER_GUARD_RC rc=$GUARD_RC (grep rc 1 = pass; anything else = fail)"; exit 94
+          fi
           if [ -n "$(find /tmp/$PREFIX-out -mindepth 1 -maxdepth 1 ! -name 'NON_CERTIFYING_REHEARSAL*' -print -quit)" ]; then
             echo "E_UNPREFIXED_TOPLEVEL_EVIDENCE"; exit 94
           fi
@@ -711,7 +919,7 @@
           # K2 (peer run-12 K4 containment): fail closed if any private-key material stands
           # under ANY evidence upload path - by NAME (*.key/*.pem/*.p12/*.pfx) or CONTENT
           # (PEM "PRIVATE KEY" markers, PKCS#8/RSA DER key headers). Upload only if this passed.
-          printf '%s\n' "$SCAN_PATHS" > /tmp/$PREFIX-k2-paths.txt
+          printf '%s\n' "$SCAN_PATHS" "/tmp/$PREFIX-inrun" > /tmp/$PREFIX-k2-paths.txt
           python3 - /tmp/$PREFIX-k2-paths.txt <<'PYK'
           import sys, os, re
           paths = [l.strip() for l in open(sys.argv[1]) if l.strip()]
@@ -774,10 +982,10 @@
   # at the same canonical in-container path /build with identical pinned inputs. The FD
   # hash must equal the first runner's. The Build tree is kept and every module's
   # CodeView PDB path is parsed: all absolute paths must carry the /build/ prefix.
-  certification-env2:
+  rehearsal-env2:
     runs-on: ubuntu-22.04
     timeout-minutes: 360
-    needs: certification
+    needs: rehearsal
     steps:
       - name: NON_CERTIFYING_REHEARSAL ref+attempt fail-closed gate
         env:
@@ -787,7 +995,10 @@
         run: |
           set -euo pipefail
           [ "${{ github.run_attempt }}" = "1" ] || { echo "E_RERUN_FORBIDDEN run_attempt=${{ github.run_attempt }}"; exit 89; }
-          [ "${{ github.ref }}" = "refs/heads/p3-certification" ] || { echo "E_REF_MISMATCH ref=${{ github.ref }}"; exit 89; }
+          case "${{ github.ref }}" in
+            refs/heads/p3-rehearsal-3|refs/heads/p3-rehearsal-4) : ;;
+            *) echo "E_REF_MISMATCH ref=${{ github.ref }}"; exit 89 ;;
+          esac
           # pushed-once gate (T3 F5): the ref must be created by THIS push, never force-pushed
           [ "$EV_CREATED" = "true" ] || { echo "E_PUSHED_ONCE created=$EV_CREATED"; exit 89; }
           [ "$EV_FORCED" = "false" ] || { echo "E_PUSHED_ONCE forced=$EV_FORCED"; exit 89; }
@@ -802,8 +1013,7 @@
           # D2 gate (reviewer ruling): every relative file/script argument used by later
           # steps must exist in the committed tree. Fixed list, fail-closed exit 88.
           cd docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
-          [ -e evidence/successor-to-certify.efi ] || { echo "E_SIGNED_UKI_ABSENT evidence/successor-to-certify.efi (post-signing state: the owner-signed UKI is committed and hash-pinned; this fires only if the file vanishes - no fallback)"; exit 90; }
-          for f in             platform.lock.json             config.json             stage-platform.sh             make-shims.sh             preflight-check.py             build-ovmf-debug.sh             scan-pe-pdb-paths.py             build-enroll-app.sh             bwrap-argv.sh             enroll-predicate-check.py             rehearsal-enroll.sh             ../build-esp-image.sh             run-ceremony.sh; do
+          for f in             platform.lock.json             config.json             stage-platform.sh             make-shims.sh             preflight-check.py             build-ovmf-debug.sh             scan-pe-pdb-paths.py             build-enroll-app.sh             bwrap-argv.sh             enroll-predicate-check.py             rehearsal-enroll.sh             ../build-esp-image.sh             evidence/successor-signed.efi             evidence/successor-to-certify.efi             run-ceremony.sh             config_schema.py             bind-fixture-certs.sh; do
             [ -e "$f" ] || { echo "E_WORKFLOW_ARG_MISSING $f"; exit 88; }
           done
       - name: NON_CERTIFYING_REHEARSAL env2 host inputs declared (B3)
@@ -896,12 +1106,22 @@
           set -euo pipefail
           cd docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
           export PATH="/tmp/$PREFIX-stage-env2/shims:$PATH"
-          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-to-certify.efi /tmp/$PREFIX-esp-env2-a 0ea8dd7da95eece8fe8eefed9f3fb701b9cdfd09abe5cfdd5f1954dd0d2f6959 21156176 c5-successor-to-certify-esp.raw
-          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-to-certify.efi /tmp/$PREFIX-esp-env2-b 0ea8dd7da95eece8fe8eefed9f3fb701b9cdfd09abe5cfdd5f1954dd0d2f6959 21156176 c5-successor-to-certify-esp.raw
-          cmp /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw /tmp/$PREFIX-esp-env2-b/c5-successor-to-certify-esp.raw
-          HE=$(sha256sum /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw | cut -d' ' -f1)
-          [ "$HE" = "6ff0d9ef89668ef79e60eba41eb70704664008697637797b384e6e67075d2f77" ] || { echo "E_ESP_PIN_MISMATCH $HE"; exit 46; }
-          sha256sum /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw > /tmp/$PREFIX-env2-evidence/NON_CERTIFYING_REHEARSAL-env2-esp-hash.txt
+          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-signed.efi /tmp/$PREFIX-esp-env2-a 133096976ee70a8c272cbcc8c28d369bfc65d93d994cbff4174b947df00239d1 21166416 c5-root-admitter-uki-v3-esp.raw
+          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-signed.efi /tmp/$PREFIX-esp-env2-b 133096976ee70a8c272cbcc8c28d369bfc65d93d994cbff4174b947df00239d1 21166416 c5-root-admitter-uki-v3-esp.raw
+          cmp /tmp/$PREFIX-esp-env2-a/c5-root-admitter-uki-v3-esp.raw /tmp/$PREFIX-esp-env2-b/c5-root-admitter-uki-v3-esp.raw
+          HE=$(sha256sum /tmp/$PREFIX-esp-env2-a/c5-root-admitter-uki-v3-esp.raw | cut -d' ' -f1)
+          [ "$HE" = "2bfa621238890890765c9636f99dabfb46233bfa626afb580ce33e65858019c9" ] || { echo "E_ESP_PIN_MISMATCH $HE"; exit 46; }
+          sha256sum /tmp/$PREFIX-esp-env2-a/c5-root-admitter-uki-v3-esp.raw > /tmp/$PREFIX-env2-evidence/NON_CERTIFYING_REHEARSAL-env2-esp-hash.txt
+          # peer pre-push ruling on 63dc0ed9 (revision 2): env2 reproduces the SLOT ESP too.
+          # peer run-36081199933 ruling (item 1): SEPARATE FRESH dirs + copy + pin check.
+          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-to-certify.efi /tmp/$PREFIX-esp-env2-slot-a 0ea8dd7da95eece8fe8eefed9f3fb701b9cdfd09abe5cfdd5f1954dd0d2f6959 21156176 c5-successor-to-certify-esp.raw
+          sudo unshare -n env PATH="$PATH" ../build-esp-image.sh evidence/successor-to-certify.efi /tmp/$PREFIX-esp-env2-slot-b 0ea8dd7da95eece8fe8eefed9f3fb701b9cdfd09abe5cfdd5f1954dd0d2f6959 21156176 c5-successor-to-certify-esp.raw
+          cmp /tmp/$PREFIX-esp-env2-slot-a/c5-successor-to-certify-esp.raw /tmp/$PREFIX-esp-env2-slot-b/c5-successor-to-certify-esp.raw
+          HS=$(sha256sum /tmp/$PREFIX-esp-env2-slot-a/c5-successor-to-certify-esp.raw | cut -d' ' -f1)
+          [ "$HS" = "6ff0d9ef89668ef79e60eba41eb70704664008697637797b384e6e67075d2f77" ] || { echo "E_ESP_SLOT_PIN_MISMATCH $HS"; exit 46; }
+          sudo cp --sparse=always /tmp/$PREFIX-esp-env2-slot-a/c5-successor-to-certify-esp.raw /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw
+          [ "$(sha256sum /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw | cut -d' ' -f1)" = "6ff0d9ef89668ef79e60eba41eb70704664008697637797b384e6e67075d2f77" ] || { echo "E_SLOT_ESP_COPY_PIN /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw"; exit 46; }
+          sha256sum /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw > /tmp/$PREFIX-env2-evidence/NON_CERTIFYING_REHEARSAL-env2-esp-slot-hash.txt
       - name: NON_CERTIFYING_REHEARSAL env2 accepted-firmware pins + runner-1 hash echo
         run: |
           set -euo pipefail
@@ -936,10 +1156,14 @@
           A1=$(grep -- "-app-a/enroll-app.efi" /tmp/$PREFIX-fw1/$PREFIX-fw-hashes.txt | cut -d' ' -f1)
           A2=$(sha256sum /tmp/$PREFIX-app-env2-a/enroll-app.efi | cut -d' ' -f1)
           [ "$A1" = "$A2" ] || { echo "E_ENV2_APP_MISMATCH $A1 $A2"; exit 93; }
-          E1=$(grep -- "-esp-a/c5-successor-to-certify-esp.raw" /tmp/$PREFIX-fw1/$PREFIX-fw-hashes.txt | cut -d' ' -f1)
-          E2=$(sha256sum /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw | cut -d' ' -f1)
+          E1=$(grep -- "-esp-a/c5-root-admitter-uki-v3-esp.raw" /tmp/$PREFIX-fw1/$PREFIX-fw-hashes.txt | cut -d' ' -f1)
+          E2=$(sha256sum /tmp/$PREFIX-esp-env2-a/c5-root-admitter-uki-v3-esp.raw | cut -d' ' -f1)
           [ "$E1" = "$E2" ] || { echo "E_ENV2_ESP_MISMATCH $E1 $E2"; exit 93; }
-          echo "env2 reproduction byte-identical across runners at canonical /build (firmware + enroll app + ESP image)"
+          # peer pre-push ruling on 63dc0ed9 (revision 2): the SLOT ESP crosses runners too.
+          S1=$(grep -- "-esp-a/c5-successor-to-certify-esp.raw" /tmp/$PREFIX-fw1/$PREFIX-fw-hashes.txt | cut -d' ' -f1)
+          S2=$(sha256sum /tmp/$PREFIX-esp-env2-a/c5-successor-to-certify-esp.raw | cut -d' ' -f1)
+          [ "$S1" = "$S2" ] || { echo "E_ENV2_ESP_SLOT_MISMATCH $S1 $S2"; exit 93; }
+          echo "env2 reproduction byte-identical across runners at canonical /build (firmware + enroll app + ESP image + slot ESP image)"
       - name: NON_CERTIFYING_REHEARSAL env2 evidence manifest + digest (peer N9)
         run: |
           set -euo pipefail
@@ -972,7 +1196,7 @@
           # K2 (peer run-12 K4 containment): fail closed if any private-key material stands
           # under ANY evidence upload path - by NAME (*.key/*.pem/*.p12/*.pfx) or CONTENT
           # (PEM "PRIVATE KEY" markers, PKCS#8/RSA DER key headers). Upload only if this passed.
-          printf '%s\n' "$SCAN_PATHS" > /tmp/$PREFIX-k2-paths.txt
+          printf '%s\n' "$SCAN_PATHS" "/tmp/$PREFIX-inrun" > /tmp/$PREFIX-k2-paths.txt
           python3 - /tmp/$PREFIX-k2-paths.txt <<'PYK'
           import sys, os, re
           paths = [l.strip() for l in open(sys.argv[1]) if l.strip()]
@@ -1023,133 +1247,9 @@
           path: *env2_evidence_upload_paths
           retention-days: 90
           if-no-files-found: error
-
       - name: env2 checkout immutability gate (end of job, frozen SHA)
         if: "!cancelled()"
         run: |
           set -euo pipefail
           R=docs/verified-architecture-phase2-v3/provisioning/p3/rehearsal
           "$R/checkout-gate.sh" "$GITHUB_WORKSPACE" "$R/prep"
-
-  # Marker emission: ONLY reachable when every prior step of both jobs succeeded. The
-  # marker is bound to the certified commit SHA, run id and ref on stdout AND in the
-  # marker file, so the artifact cannot be transplanted onto another commit or run.
-  # The marker line is emitted EXACTLY ONCE to stdout and EXACTLY ONCE into the evidence
-  # marker file, each immediately followed by the full non-claim block. The marker file
-  # lives under the rehearsal-prefixed scratch tree ($PREFIX-out) like every other
-  # ceremony output; only the artifact NAME carries the certification label.
-  certification-marker:
-    runs-on: ubuntu-22.04
-    timeout-minutes: 10
-    needs: [certification, certification-env2]
-    steps:
-      - name: NON_CERTIFYING_REHEARSAL ref+attempt fail-closed gate
-        env:
-          EV_CREATED: ${{ github.event.created }}
-          EV_FORCED: ${{ github.event.forced }}
-          EV_BEFORE: ${{ github.event.before }}
-        run: |
-          set -euo pipefail
-          [ "${{ github.run_attempt }}" = "1" ] || { echo "E_RERUN_FORBIDDEN run_attempt=${{ github.run_attempt }}"; exit 89; }
-          [ "${{ github.ref }}" = "refs/heads/p3-certification" ] || { echo "E_REF_MISMATCH ref=${{ github.ref }}"; exit 89; }
-          [ "$EV_CREATED" = "true" ] || { echo "E_PUSHED_ONCE created=$EV_CREATED"; exit 89; }
-          [ "$EV_FORCED" = "false" ] || { echo "E_PUSHED_ONCE forced=$EV_FORCED"; exit 89; }
-          [ "$EV_BEFORE" = "0000000000000000000000000000000000000000" ] || { echo "E_PUSHED_ONCE before=$EV_BEFORE"; exit 89; }
-      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4.3.0
-        with:
-          name: NON_CERTIFYING_REHEARSAL-evidence
-          path: /tmp/NON_CERTIFYING_REHEARSAL-ev1
-      - name: NON_CERTIFYING_REHEARSAL marker emission (only reached on full suite success)
-        run: |
-          set -euo pipefail
-          mkdir -p /tmp/$PREFIX-out
-          # batch1r3 C8: the marker binds the ceremony evidence-manifest sha256 + workflow path
-          MANIFEST=/tmp/$PREFIX-ev1/NON_CERTIFYING_REHEARSAL-out/NON_CERTIFYING_REHEARSAL-manifest.json
-          [ -f "$MANIFEST" ] || { echo "E_EVIDENCE_MANIFEST_MISSING"; exit 99; }
-          MANIFEST_SHA=$(sha256sum "$MANIFEST" | cut -d' ' -f1)
-          printf '%s\n%s\n%s\n%s\n%s\n%s\n%s\n%s\n%s\n%s\nSHA=%s\nRUN_ID=%s\nREF=%s\n' \
-            'NON_CERTIFYING_REHEARSAL_PASS' \
-            'SCOPE: QEMU/KVM + reproducible DEBUG OVMF Secure Boot certification of the owner-signed UKI in evidence/successor-to-certify.efi only.' \
-            'EXPECTED-PASS: firmware Secure Boot accept + kernel exec + signed-init exit-98 (E_PROVIDER_NAMESPACE/E_PROVIDER_LINK_MISSING on non-Google hardware).' \
-            'NOT a full runtime PASS. NOT a dm-verity runtime PASS. NOT a provisioning PASS.' \
-            'NOT a Google platform certification. NOT a complete boot-chain certification.' \
-            'NO Google firmware, vTPM, GCP-deployment or persistence claims.' \
-            'Firmware: accepted canonical DEBUG OVMF (fc150336fce10eb5508fa440cdf7300622c8c3ac71f2fbe10a0731422b9cef24).' \
-            'Environment: GitHub-hosted ubuntu-22.04 standard runner, KVM, attempt 1, ref p3-certification.' \
-            'WORKFLOW=.github/workflows/NON_CERTIFYING_REHEARSAL-CERTIFICATION-workflow.yml' \
-            "EVIDENCE_MANIFEST_SHA256=$MANIFEST_SHA" \
-            "${{ github.sha }}" "${{ github.run_id }}" "${{ github.ref }}"
-          printf '%s\n%s\n%s\n%s\n%s\n%s\n%s\n%s\n%s\n%s\nSHA=%s\nRUN_ID=%s\nREF=%s\n' \
-            'NON_CERTIFYING_REHEARSAL_PASS' \
-            'SCOPE: QEMU/KVM + reproducible DEBUG OVMF Secure Boot certification of the owner-signed UKI in evidence/successor-to-certify.efi only.' \
-            'EXPECTED-PASS: firmware Secure Boot accept + kernel exec + signed-init exit-98 (E_PROVIDER_NAMESPACE/E_PROVIDER_LINK_MISSING on non-Google hardware).' \
-            'NOT a full runtime PASS. NOT a dm-verity runtime PASS. NOT a provisioning PASS.' \
-            'NOT a Google platform certification. NOT a complete boot-chain certification.' \
-            'NO Google firmware, vTPM, GCP-deployment or persistence claims.' \
-            'Firmware: accepted canonical DEBUG OVMF (fc150336fce10eb5508fa440cdf7300622c8c3ac71f2fbe10a0731422b9cef24).' \
-            'Environment: GitHub-hosted ubuntu-22.04 standard runner, KVM, attempt 1, ref p3-certification.' \
-            'WORKFLOW=.github/workflows/NON_CERTIFYING_REHEARSAL-CERTIFICATION-workflow.yml' \
-            "EVIDENCE_MANIFEST_SHA256=$MANIFEST_SHA" \
-            "${{ github.sha }}" "${{ github.run_id }}" "${{ github.ref }}" \
-            > /tmp/$PREFIX-out/NON_CERTIFYING_REHEARSAL-certification-marker.txt
-      - name: zero-private-key evidence gate K2 before marker upload (peer run-12 K4)
-        id: k2gate-marker
-        if: '!cancelled()'
-        env:
-          SCAN_PATHS: ${{ env.EVIDENCE_UPLOAD_PATHS }}
-        run: |
-          set -euo pipefail
-          # K2 (peer run-12 K4 containment): fail closed if any private-key material stands
-          # under ANY evidence upload path - by NAME (*.key/*.pem/*.p12/*.pfx) or CONTENT
-          # (PEM "PRIVATE KEY" markers, PKCS#8/RSA DER key headers). Upload only if this passed.
-          printf '%s\n' "$SCAN_PATHS" > /tmp/$PREFIX-k2-paths.txt
-          python3 - /tmp/$PREFIX-k2-paths.txt <<'PYK'
-          import sys, os, re
-          paths = [l.strip() for l in open(sys.argv[1]) if l.strip()]
-          namehit, conthit, linkhit = [], [], []
-          pem = b"PRIVATE KEY"
-          der_rsa = re.compile(rb"\x30\x82..\x02\x01\x00\x02\x82", re.S)
-          der_p8 = re.compile(rb"\x30\x82..\x02\x01\x00\x30", re.S)
-          scanned = [0]
-          def _k2raise(e):
-              print("E_K2_SWEEP_UNREADABLE %s" % e); sys.exit(94)
-          def scan_file(p):
-              scanned[0] += 1
-              if re.search(r"\.(key|pem|p12|pfx)$", os.path.basename(p), re.I):
-                  namehit.append(p); return
-              b = open(p, "rb").read()
-              if pem in b or der_rsa.search(b) or der_p8.search(b):
-                  conthit.append(p)
-          for p in paths:
-              if os.path.islink(p):
-                  linkhit.append(p); continue
-              if os.path.isfile(p):
-                  scan_file(p)
-              elif os.path.isdir(p):
-                  for base, dirs, files in os.walk(p, onerror=_k2raise):
-                      for dn in dirs:
-                          if os.path.islink(os.path.join(base, dn)):
-                              linkhit.append(os.path.join(base, dn))
-                      for fn in files:
-                          full = os.path.join(base, fn)
-                          if os.path.islink(full):
-                              linkhit.append(full); continue
-                          scan_file(full)
-          if linkhit:
-              print("E_SYMLINK_IN_EVIDENCE symlinks=%r" % linkhit[:3])
-              sys.exit(94)
-          if namehit or conthit:
-              print("E_PRIVATE_KEY_IN_EVIDENCE names=%r content=%r" % (namehit[:3], conthit[:3]))
-              sys.exit(94)
-          if scanned[0] == 0:
-              print("E_K2_SWEEP_EMPTY no files scanned across %d upload paths" % len(paths)); sys.exit(94)
-          print("zero-private-key gate: clean across %d upload paths (scanned=%d)" % (len(paths), scanned[0]))
-          PYK
-      - name: NON_CERTIFYING_REHEARSAL marker upload (ephemeral CI artifact only)
-        if: always() && steps.k2gate-marker.outcome == 'success'
-        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4.6.2
-        with:
-          name: NON_CERTIFYING_REHEARSAL-marker
-          path: /tmp/NON_CERTIFYING_REHEARSAL-out/NON_CERTIFYING_REHEARSAL-certification-marker.txt
-          retention-days: 90
-          if-no-files-found: error
