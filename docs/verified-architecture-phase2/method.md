# Phase 2 real-scene geometry validation

The binding decider is authored physical geometry recovered in the locked production source. Panorama and PNG bytes are pinned but diagnostic only: availability and pixels do not prove geometry or lineage. The gate consumes full 3D bounds, exact Surface Model revision/digest, explicit positive regions and exact contact plane. It never clamps, snaps, repairs, substitutes or falls back. Unknown support semantics fail closed.

School, clinic and urban only. Industrial is excluded until geometry is pinned. Synagogue remains UNKNOWN. No runtime, production, asset or Phase 1 file is changed. PASS means this offline Geometry Gate accepted one proposed transform against the pinned authored geometry; `placementReady` remains false and no promotion API exists.
