# Gate C contract

One AUTHORED_NEW treatment-chair body and one seat-only entity-owned support surface are defined. Both physical definitions exclude lifecycle state; separate A.2 envelopes stop at VALIDATED pending USER_GATE_REVIEW.

A support dependency binds the supported entity to exact owner entity revision, exact owner body revision/digest, exact seat surface revision/digest and candidate world revision. Candidate dependencies are sorted by entity ID, validated before geometry, and fail closed. Mutation commands are sorted deterministically and applied to one candidate world before validation; one event and one authoritative commit follow only if all candidate entities pass. Rejection keeps the prior state digest. There is no auto-drop, move, detach, snap or clamp.

The School adapter recognizes only the exact treatment-chair body and the synthetic contract-only supported box. It materializes the seat surface from exact owner translation with identity orientation, then sends the dependent to the unchanged Phase 2 Geometry Gate. Support code binds evidence but does not grant geometry PASS. The real adult casualty stays MALE and SUPINE_FLOOR.
