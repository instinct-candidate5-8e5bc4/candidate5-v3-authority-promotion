# Matrices, Performance Protocol and UNKNOWN Ledger

## Positive matrix

Identity; translation only; rotation only; translation+rotation; nested translations; nested rotations; mixed nesting; q/-q equality; ratio reduction; exact identity/90/180/non-axis matrices; World→Entity→Feature→Body→Contact; parent invalidation; insertion-order independence; repeated resolution/materialization/digest; exact associativity before quantization; horizontal/vertical/inclined/upside-down transform representation.

## Negative matrix

Malformed/unsafe/noncanonical integer, negative zero, zero quaternion, malformed arity/component, strict noncanonical ratio, denominator zero/negative normalization boundary, stored/intermediate/output overflow, impossible precision, malformed translation, duplicate frame, zero/multiple WorldFrames, missing/dangling parent, stale revision, wrong digest, tampered frame digest, direct/indirect cycle, count/depth limit, stale snapshot and materially different transform digest inequality. Every failure is deterministic; no correction, wrap, saturation or fallback.

## Performance protocol

On one named Node/OS/CPU environment, measure p50/p95/max microseconds for quaternion canonicalization, exact matrix derivation, depth-64 frame validation, rational reduction, rational-to-fixed materialization and digest. For composition depths 1/2/4/8/16/32/64, report resolution p50/p95/max, maximum translation numerator bits, denominator bits, quaternion component bits and serialized snapshot bytes. No thresholds.

## UNKNOWN ledger

- Whether 63/256/4096-bit and depth-64 limits fit every future approved scenario.
- Worst-case adversarial growth within depth bounds and denial-of-service controls beyond count/depth/bit checks.
- Phase 2 V3 error budget, quantization policy acceptance and geometry primitive scale.
- Imported asset/authoring conversion into integer quaternion ratios and evidence workflow.
- Exact representation of externally specified degree angles such as 30 degrees; approximation selection is unapproved.
- Browser/runtime BigInt performance across iPhone, Android, tablet and desktop.
- Frame graph incremental validation/materialization cache design.
- Oriented geometry, collision, supports, vehicles, stairs, ramps and renderer binding.

None is promoted.
