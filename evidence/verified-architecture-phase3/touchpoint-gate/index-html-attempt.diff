diff --git a/index.html b/index.html
index cc4f11e..27e16a6 100644
--- a/index.html
+++ b/index.html
@@ -273,7 +273,7 @@ if(params.has('nlusuite'))setTimeout(()=>{const tests=[['תבדוק אם המק
  const media=matchMedia('(prefers-reduced-motion: reduce)');if(media.matches&&!ctl.getSettings().reduced)ctl.setSetting('reduced',true);
  window.KoA11y={announce,controller:ctl};
 })();
-</script><script>window.__applyAdaptiveQuality=()=>{};const adaptiveRuntime=window.KoRishonAdaptiveRuntime.createRuntime(window,{onQualityChange:(tier,settings)=>window.__applyAdaptiveQuality(tier,settings)});window.__adaptiveRuntime=adaptiveRuntime;document.body.dataset.capabilityprofile=JSON.stringify(adaptiveRuntime.profile);document.body.dataset.degradation=JSON.stringify(adaptiveRuntime.degradation);document.body.dataset.quality=adaptiveRuntime.quality.tier;let adaptiveLast=performance.now();function adaptiveSample(){const now=performance.now();adaptiveRuntime.quality.sample(now-adaptiveLast,now);adaptiveLast=now;requestAnimationFrame(adaptiveSample)}requestAnimationFrame(adaptiveSample);const markUiInteractive=()=>{if(document.body.dataset.uiinteractive)return;adaptiveRuntime.markInteractive();document.body.dataset.uiinteractive='pass';document.body.dataset.adaptivetest='pass';document.body.dataset.adaptivesnapshot=JSON.stringify(adaptiveRuntime.snapshot())};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',markUiInteractive,{once:true});else markUiInteractive();window.exportAdaptiveAudit=()=>adaptiveRuntime.snapshot();window.runRepresentativePerformanceAudit=({durationMs=5000}={})=>new Promise(resolve=>{const start=performance.now(),frames=[];let previous=start;function tick(now){frames.push(now-previous);previous=now;if(now-start<durationMs){requestAnimationFrame(tick);return}const sorted=frames.slice(1).sort((a,b)=>a-b),avg=sorted.reduce((a,b)=>a+b,0)/Math.max(1,sorted.length),p95=sorted[Math.min(sorted.length-1,Math.floor(sorted.length*.95))]||0,fps=avg?1000/avg:0;adaptiveRuntime.budget.record('renderFps',fps);const result={durationMs:now-start,frames:sorted.length,fps,p95FrameMs:p95,memoryMb:KoRishonAdaptiveRuntime.memoryUsageMb(window),snapshot:adaptiveRuntime.snapshot(),assets:JSON.parse(document.body.dataset.assettimings||'[]')};document.body.dataset.performanceaudit=JSON.stringify(result);resolve(result)}requestAnimationFrame(tick)});</script><script type="importmap">{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.169.0/build/three.module.js"}}</script><script type="module">
+</script><script>window.__applyAdaptiveQuality=()=>{};const adaptiveRuntime=window.KoRishonAdaptiveRuntime.createRuntime(window,{onQualityChange:(tier,settings)=>window.__applyAdaptiveQuality(tier,settings)});window.__adaptiveRuntime=adaptiveRuntime;document.body.dataset.capabilityprofile=JSON.stringify(adaptiveRuntime.profile);document.body.dataset.degradation=JSON.stringify(adaptiveRuntime.degradation);document.body.dataset.quality=adaptiveRuntime.quality.tier;let adaptiveLast=performance.now();function adaptiveSample(){const now=performance.now();adaptiveRuntime.quality.sample(now-adaptiveLast,now);adaptiveLast=now;requestAnimationFrame(adaptiveSample)}requestAnimationFrame(adaptiveSample);const markUiInteractive=()=>{if(document.body.dataset.uiinteractive)return;adaptiveRuntime.markInteractive();document.body.dataset.uiinteractive='pass';document.body.dataset.adaptivetest='pass';document.body.dataset.adaptivesnapshot=JSON.stringify(adaptiveRuntime.snapshot())};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',markUiInteractive,{once:true});else markUiInteractive();window.exportAdaptiveAudit=()=>adaptiveRuntime.snapshot();window.runRepresentativePerformanceAudit=({durationMs=5000}={})=>new Promise(resolve=>{const start=performance.now(),frames=[];let previous=start;function tick(now){frames.push(now-previous);previous=now;if(now-start<durationMs){requestAnimationFrame(tick);return}const sorted=frames.slice(1).sort((a,b)=>a-b),avg=sorted.reduce((a,b)=>a+b,0)/Math.max(1,sorted.length),p95=sorted[Math.min(sorted.length-1,Math.floor(sorted.length*.95))]||0,fps=avg?1000/avg:0;adaptiveRuntime.budget.record('renderFps',fps);const result={durationMs:now-start,frames:sorted.length,fps,p95FrameMs:p95,memoryMb:KoRishonAdaptiveRuntime.memoryUsageMb(window),snapshot:adaptiveRuntime.snapshot(),assets:JSON.parse(document.body.dataset.assettimings||'[]')};document.body.dataset.performanceaudit=JSON.stringify(result);resolve(result)}requestAnimationFrame(tick)});</script><script type="importmap">{"imports":{"three":"https://cdn.jsdelivr.net/npm/three@0.169.0/build/three.module.js"}}</script><script type="module" src="./tools/verified-architecture-phase3/observer-preload.js"></script><script type="module">
 import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.169.0/build/three.module.js';
 import {GLTFLoader} from 'https://cdn.jsdelivr.net/npm/three@0.169.0/examples/jsm/loaders/GLTFLoader.js';
 import * as SkeletonUtils from 'https://cdn.jsdelivr.net/npm/three@0.169.0/examples/jsm/utils/SkeletonUtils.js';
