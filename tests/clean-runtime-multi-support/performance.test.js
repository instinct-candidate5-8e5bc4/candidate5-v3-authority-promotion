'use strict';const test=require('node:test'),H=require('./helpers'),M=require('../../src/clean-runtime/multi-support/relations'),os=require('node:os');function measure(fn,n=30){const a=[];for(let i=0;i<n;i++){const t=process.hrtime.bigint();fn(i);a.push(Number(process.hrtime.bigint()-t)/1000)}a.sort((x,y)=>x-y);return {unit:'microseconds',count:n,min:a[0],p50:a[15],p95:a[28],max:a.at(-1)}}test('measurement-only multi-support costs',()=>{const sets=[[H.relation('a')],[H.relation('a'),H.relation('b','wall-contact','wall')],[H.relation('a'),H.relation('b','wall-contact','wall'),H.relation('c','third-contact')]];const validate=sets.map(rs=>measure(()=>M.validateRelations(rs,H.initial(rs),{targetWorldRevision:0}))),index=measure(()=>M.buildIndex(sets[2])),transaction=measure(i=>H.runtime().proposeTransaction({transactionId:'t'+i,expectedWorldRevision:0,commands:[H.cmd('AttachSupportRelation','a',{relation:H.relation('a','floor-contact','floor',1)})]}));console.log(JSON.stringify({environment:{node:process.version,platform:process.platform,arch:process.arch,cpu:os.cpus()[0].model},threshold:null,validateOneTwoThree:validate,indexRebuild:index,transaction}))});
