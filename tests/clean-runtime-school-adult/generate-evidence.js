'use strict';const H=require('./helpers'),{canonicalBytes}=require('../../src/clean-runtime/contracts/canonical');const cases=[['legal',0,0,1],['floating',0,.01,1],['wall',6.8,0,1],['door',5.4,0,-3.2],['partial-door',4.5,0,-3.2],['locker',-4,0,-3],['partial-outside',0,0,5.5],['full-outside',8,0,1]],rows=[];for(const [name,x,y,z] of cases){const api=H.runtime().worldMutationAPI,b=api.getWorldState(),r=api.proposeMutation(H.move(name,x,y,z)),a=api.getWorldState();rows.push({name,status:r.status,beforeDigest:b.stateDigest,afterDigest:a.stateDigest,worldRevision:a.revision,cleanFailure:r.failure||null,phase2Reason:r.failure?.evidence?.detail?.legality?.evidence?.phase2ReasonCode||'GEOMETRY_GATE_PASS',phase2Evidence:r.failure?.evidence?.detail?.legality?.evidence?.phase2Evidence||null,event:r.event})}function det(){const api=H.runtime().worldMutationAPI;api.proposeMutation(H.move('det',0,0,1));return {profile:H.PROFILE,posture:H.POSTURE,body:H.BODY,stateBytes:canonicalBytes(api.getWorldState()).toString('hex'),eventBytes:canonicalBytes(api.getEventLog()).toString('hex')}}process.stdout.write(JSON.stringify({rows,deterministicA:det(),deterministicB:det()},null,2)+'\n');
