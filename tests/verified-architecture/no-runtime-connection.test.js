'use strict';const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');test('existing runtime has no verified architecture connection',()=>{const root=path.join(__dirname,'../..');const files=['index.html','sw.js',...fs.readdirSync(path.join(root,'src/nextgen')).map(x=>'src/nextgen/'+x)];for(const f of files)assert(!fs.readFileSync(path.join(root,f),'utf8').includes('verified-architecture'),f);});
