'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {ScenePackageRegistry}=require('../../src/verified-architecture');const {fixture}=require('./helpers');test('immutable idempotent deterministic registry and import revalidation',()=>{const r=new ScenePackageRegistry,p=fixture('scene-01-planar-indoor.json');const out=r.register(p);assert(Object.isFrozen(out));assert.throws(()=>out.failureReasons.push('x'),TypeError);assert.deepEqual(r.register(p),out);assert.throws(()=>r.register({...p,failureReasons:['changed']}),/immutable/);const s=r.serialize(),r2=new ScenePackageRegistry;r2.import(s);assert.equal(r2.serialize(),s);});
