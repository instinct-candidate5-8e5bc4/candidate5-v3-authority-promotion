'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {validateScenePackage}=require('../../src/verified-architecture');const {fixture}=require('./helpers');test('clinic fail-5930234b remains fail-closed before render without auto-fix',()=>{const p=fixture('scene-02-multisurface-clinical.json');delete p.surfaceModelRef;let evidence={canonicalFailureId:'fail-5930234b',autoFixed:false,renderStarted:false};assert.throws(()=>validateScenePackage(p));assert.deepEqual(evidence,{canonicalFailureId:'fail-5930234b',autoFixed:false,renderStarted:false});});
