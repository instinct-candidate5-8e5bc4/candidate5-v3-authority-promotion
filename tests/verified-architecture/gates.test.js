'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {GATE_CATEGORIES,gate}=require('../../src/verified-architecture/validation-result');test('P1-G01 through P1-G18 have deterministic categories',()=>{assert.equal(Object.keys(GATE_CATEGORIES).length,18);for(const [id,c] of Object.entries(GATE_CATEGORIES))assert.deepEqual(gate(id,'FAIL').failureCategory,c);});
