'use strict';const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');test('architecture has no fixture identities, renderer types, placement constants or fallback methods',()=>{const dir=path.join(__dirname,'../../src/verified-architecture');const text=fs.readdirSync(dir).filter(x=>x.endsWith('.js')).map(x=>fs.readFileSync(path.join(dir,x),'utf8')).join('\n');for(const token of ['scene-01','scene-02','scene-03','school-treatment-room','clinical-treatment-room','road-traffic-accident-site','THREE.Sprite'])assert(!text.includes(token),token);assert(!/function\s+(promote|approve|certifyReady|setReady)/.test(text));});
