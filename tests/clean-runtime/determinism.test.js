'use strict';const test=require('node:test'),assert=require('node:assert/strict');const {system,spawn}=require('./helpers');const {canonicalBytes}=require('../../src/clean-runtime/contracts/canonical');function run(){const {api}=system();const initial=api.getWorldState();api.proposeMutation(spawn());return {state:canonicalBytes(api.getWorldState()).toString('hex'),stateDigest:api.getWorldState().stateDigest,events:canonicalBytes(api.getEventLog()).toString('hex'),eventDigest:api.getEventLog().at(-1).eventDigest,initial}}test('same fixture repeated is byte identical',()=>assert.deepEqual(run(),run()));
