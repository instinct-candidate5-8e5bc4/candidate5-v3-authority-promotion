'use strict';const {runtime,move,bagEntity}=require('./helpers'),{canonicalBytes}=require('../../src/clean-runtime/contracts/canonical');
const defs=[['legal',-3,.175,1],['floating',-3,.18,1],['door',5.4,.175,-3.84],['partial-door',4.5,.175,-3.84],['locker',-4,.175,-3.5],['partial-outside',0,.175,5.9],['full-outside',8,.175,1]];const rows=[];
for(const [name,x,y,z] of defs){const api=runtime().worldMutationAPI,before=api.getWorldState(),result=api.proposeMutation(move(name,x,y,z)),after=api.getWorldState();rows.push({name,beforeDigest:before.stateDigest,afterDigest:after.stateDigest,status:result.status,worldRevision:after.revision,committedTransform:after.entities['school-medical-bag'].transform,cleanFailure:result.failure?.code||null,underlyingPhase2Reason:result.failure?.evidence?.detail?.legality?.evidence?.phase2ReasonCode||'GEOMETRY_GATE_PASS',phase2Evidence:result.failure?.evidence?.detail?.legality?.evidence?.phase2Evidence||null,event:result.event})}
const deterministic=()=>{const api=runtime().worldMutationAPI;api.proposeMutation(move('deterministic',-3,.175,1));return {stateBytes:canonicalBytes(api.getWorldState()).toString('hex'),eventBytes:canonicalBytes(api.getEventLog()).toString('hex'),stateDigest:api.getWorldState().stateDigest,eventDigest:api.getEventLog()[0].eventDigest}};process.stdout.write(JSON.stringify({rows,deterministicA:deterministic(),deterministicB:deterministic()},null,2)+'\n');
